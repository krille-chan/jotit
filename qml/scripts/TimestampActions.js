// File: LayoutActions.js
// Description: Actions for all Qml Pages and Components

function tsToString ( timestamp ) {
    return new Date(timestamp).toLocaleString(Qt.locale(), Locale.ShortFormat)
}
